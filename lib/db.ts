export const dbGetUserByEmail = async (email: string) => {
  // TODO: connect to your database
  return null;
};

export const dbCreateUser = async (email: string, hashedPassword: string) => {
  // TODO: connect to your database
  return { email };
};
